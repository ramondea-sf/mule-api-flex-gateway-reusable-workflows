# Controle de Versões da API
# Este arquivo mantém o histórico de versões publicadas em cada ambiente

# Versão atual em desenvolvimento
current: "1.0.0"

# Histórico de versões por ambiente
environments:
  dev:
    current: "1.0.0"
    history:
      - version: "1.0.0"
        deployedAt: "2025-11-14T00:00:00Z"
        deployedBy: "github-actions"
        commitHash: ""
        status: "active"
        
  staging:
    current: ""
    history: []
    
  prod:
    current: ""
    history: []

# Regras de versionamento
# SEMVER: X.Y.Z
# X (major): Mudanças incompatíveis na API
# Y (minor): Novas funcionalidades compatíveis
# Z (patch): Correções de bugs compatíveis

# Como atualizar a versão:
# 1. Edite o campo 'current' acima com a nova versão
# 2. Commit e push para a branch correspondente
# 3. A pipeline irá publicar automaticamente a nova versão

